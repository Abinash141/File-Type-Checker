# File-Type-Checker
A file extension  checker that checks and verify the raw bytes and compare them to known signature of file types, It also shows 16 bytes HEX, also verify file type of ".jpeg" and ".png"" 
